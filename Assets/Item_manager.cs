using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Item_manager : MonoBehaviour
{
    //創立物品清單
    //需設為public因為對應物件會用到

    // Start is called before the first frame update
    void Start()
    {
        //玩家物品欄清空(防bug，可不寫)
    }

    // Update is called once per frame
    void Update()
    {
        //辨認玩家是否持有某個物品
        //若持有(拿起)道具則清單的對應項目設為true
    }
}
